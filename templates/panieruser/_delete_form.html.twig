<form method="post" action="{{ path('panieruser_delete', {'idPanieruser': panieruser.idPanieruser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panieruser.idPanieruser) }}">
    <button class="btn">Delete</button>
</form>
