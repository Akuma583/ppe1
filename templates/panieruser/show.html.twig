{% extends 'base.html.twig' %}

{% block title %}Panieruser{% endblock %}

{% block body %}
    <h1>Panieruser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPanieruser</th>
                <td>{{ panieruser.idPanieruser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panieruser_index') }}">back to list</a>

    <a href="{{ path('panieruser_edit', {'idPanieruser': panieruser.idPanieruser}) }}">edit</a>

    {{ include('panieruser/_delete_form.html.twig') }}
{% endblock %}
